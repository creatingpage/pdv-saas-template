export const SYSTEM_CONFIG = {
  appName: "PDV Matriz",
  tagline: "Sistema de automação comercial",
  logoUrl: "/logo.png",
  primaryColor: "#1E293B",
  accentColor: "#3B82F6",
};

export function appColor(alpha = 1) {
  return `rgba(59, 130, 246, ${alpha})`;
}
